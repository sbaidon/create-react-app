import React from 'react'
{{#if hasMessages}}
import { shallowWithIntl } from 'utils/intl-enzyme'
{{else}}
import { shallow } from 'enzyme'
{{/if}}

import {{properCase name}} from 'modules/{{properCase name}}/view'

describe('<{{properCase name}} />', () => {
  it('should have tests', () => {
    {{#if hasMessages}}
    const wrapper = shallowWithIntl(<{{properCase name}} />)
    {{else}}
    const wrapper = shallow(<{{properCase name}} />)
    {{/if}}

    expect(false).toBeTruthy()
  })
})
